# Shopiva Customer Android release rules
# Keep this file intentionally minimal. R8 uses the standard Android optimizer
# rules from the Android Gradle plugin; application-specific rules can be added
# here later when required by a release build.
