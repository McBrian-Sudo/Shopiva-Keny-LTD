(function(){'use strict';
function init(el){
 if(el.dataset.niaReady)return; el.dataset.niaReady='1';
 const role=el.dataset.role||'customer';
 el.innerHTML='<div class="nia-widget-main"><div class="nia-ring"><svg viewBox="0 0 118 118"><circle cx="59" cy="59" r="50"></circle><circle class="nia-ring-live" cx="59" cy="59" r="50"></circle></svg><div class="nia-core">◉</div></div><div class="nia-copy"><div class="nia-kicker">Nia · '+role+' operations</div><h3>Nia Live Copilot</h3><p class="nia-status" data-nia-status>Idle — ready for Shopiva data.</p><div class="nia-actions"><button class="nia-action primary" data-nia-monitor type="button">🎙️ Start voice monitor</button><button class="nia-action" data-nia-refresh type="button">↻ Refresh</button></div></div></div><div class="nia-stats"><div class="nia-stat"><b data-s1>—</b><span data-l1>Orders</span></div><div class="nia-stat"><b data-s2>—</b><span data-l2>Stock</span></div><div class="nia-stat"><b data-s3>—</b><span data-l3>Today</span></div></div><div class="nia-widget-note">Live stats use the same authorized Shopiva account scope used by Nia. The waveform uses your microphone only while enabled.</div>';
 const status=el.querySelector('[data-nia-status]'),btn=el.querySelector('[data-nia-monitor]'),refresh=el.querySelector('[data-nia-refresh]');
 async function refreshStats(){try{const r=await fetch('/ai/nia/dashboard-context/',{headers:{'X-Requested-With':'XMLHttpRequest'},credentials:'same-origin'});if(!r.ok)throw new Error();const d=await r.json();if(!d.ok)throw new Error();const s=d.stats||{};el.querySelector('[data-s1]').textContent=s.primary_value??'0';el.querySelector('[data-l1]').textContent=s.primary_label||'Orders';el.querySelector('[data-s2]').textContent=s.secondary_value??'0';el.querySelector('[data-l2]').textContent=s.secondary_label||'Stock';el.querySelector('[data-s3]').textContent=s.tertiary_value??'0';el.querySelector('[data-l3]').textContent=s.tertiary_label||'Today';}catch(_){status.textContent='Live stats temporarily unavailable.'}}
 let stream=null,ctx=null,analyser=null,raf=0;
 function stop(){if(raf)cancelAnimationFrame(raf);try{stream&&stream.getTracks().forEach(t=>t.stop());ctx&&ctx.close()}catch(_){}stream=null;ctx=null;analyser=null;el.dataset.active='false';btn.textContent='🎙️ Start voice monitor';status.textContent='Idle — ready for Shopiva data.'}
 function draw(){if(!analyser)return;const data=new Uint8Array(analyser.fftSize);analyser.getByteTimeDomainData(data);let sum=0;for(let i=0;i<data.length;i++){const x=(data[i]-128)/128;sum+=x*x}const level=Math.min(1,Math.sqrt(sum/data.length)*3.2);const ring=el.querySelector('.nia-ring-live');ring.style.strokeWidth=String(5+level*9);ring.style.opacity=String(.45+level*.5);ring.style.strokeDasharray=(5+level*16)+' '+(11-level*5);raf=requestAnimationFrame(draw)}
 btn.addEventListener('click',async()=>{if(stream){stop();return}try{stream=await navigator.mediaDevices.getUserMedia({audio:true});ctx=new(window.AudioContext||window.webkitAudioContext)();analyser=ctx.createAnalyser();analyser.fftSize=512;ctx.createMediaStreamSource(stream).connect(analyser);el.dataset.active='true';btn.textContent='⏹ Stop voice monitor';status.textContent='Listening… waveform is reacting to microphone level.';draw()}catch(_){status.textContent='Microphone permission is required for the live waveform.'}});
 refresh.addEventListener('click',refreshStats);refreshStats();setInterval(refreshStats,30000);
}
function boot(){document.querySelectorAll('[data-nia-widget]').forEach(init)}
window.ShopivaNiaWidget={boot};document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot):boot();
})();