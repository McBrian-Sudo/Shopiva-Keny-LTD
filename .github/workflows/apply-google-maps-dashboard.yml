name: Apply Google Maps Customer Dashboard

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  upgrade:
    if: ${{ !contains(github.event.head_commit.message, '[skip ci]') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Apply Google Maps and dashboard upgrade
        run: python scripts/apply_google_maps_dashboard_upgrade.py

      - name: Commit generated changes
        run: |
          git config user.name "shopiva-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git diff --quiet; then
            echo "No generated changes needed."
            exit 0
          fi
          git add home/templates/accounts/dashboard.html home/templates/includes/delivery_network_map.html home/context_processors.py shopiva/settings.py render.yaml
          git commit -m "Add Google Maps delivery tracking and move notifications to top [skip ci]"
          git push
